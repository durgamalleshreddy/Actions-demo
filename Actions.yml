name: My first workflow

on: push

jobs: 
    first_job:
        runs-on: ubuntu_latest
        steps:
        -name: welcome message
        run: echo "my first github actions job"

        -name: list the files
        run: ls
        
        -name: read file
        run: cat README.md
